version: "3"
services:
  nodecook-agent:
    image: ghcr.io/nodecook/agent:dev
    container_name: nodecook-agent-dev
    restart: always
    network_mode: host
    user: root
    environment:
      - NCA_API_KEY=${NCA_API_KEY}
